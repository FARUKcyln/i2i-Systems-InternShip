import java.util.*;

public class Main {
    public static void main(String[] args) {
        List<Book> books = null;
        FavoriteBooks favoriteBooks = new FavoriteBooks();
        favoriteBooks.merge(books);
    }
}
